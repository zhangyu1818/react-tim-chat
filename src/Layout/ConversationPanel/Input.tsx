import React from 'react';

const ConversationPanelInput: React.FC = ({ children }) => (
  <section className="tim-conversation-input-area">{children}</section>
);

export default ConversationPanelInput;
