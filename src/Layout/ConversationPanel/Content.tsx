import React from 'react';

const ConversationPanelContent: React.FC = ({ children }) => (
  <section className="tim-conversation-content">{children}</section>
);

export default ConversationPanelContent;
